baseURL = "https://mtotaro.com/"
defaultContentLanguage = "en"
title = "Matteo Totaro"
theme = "LoveIt"
#themesDir = "../.."

# whether to use robots.txt
enableRobotsTXT = true
# whether to use git commit log
enableGitInfo = false
# whether to use emoji code
enableEmoji = true

[languages]
  [languages.en]  ##to add another language in the main menù i need to repeat this language.it menu.starter
    weight = 1
    # language code
    languageCode = "en"
    # website title
    title = "Matteo Totaro"
    # language name
    languageName = "English"
    # whether to include Chinese/Japanese/Korean
    hasCJKLanguage = false
    # default amount of posts in each pages
    paginate = 12
    # [UA-XXXXXXXX-X] google analytics code
    googleAnalytics = "UA-164939128-1"
    # copyright description used only for seo schema
    copyright = "This work is licensed under a Creative Commons Attribution-NonCommercial 4.0 International License."

    # Menu Info
    [languages.en.menu]
      [[languages.en.menu.main]]
        identifier = "posts"
        # you can add extra information before the name (HTML format is allowed), such as icons
        pre = ""
        # you can add extra information after the name (HTML format is supported), such as icons
        post = ""
        name = "Gallery"
        url = "/posts/"
        # title will be shown when you hover on this menu link.
        title = ""
        weight = 1
       
            
      [[languages.en.menu.main]]
        identifier = "about"
        pre = ""
        post = ""
        name = "About"
        url = "/about/"
        title = ""
        weight = 2
      
      [languages.en.params]
        # site description
        description = "Personal angle on the web with a gallery of shots presented in a very minimal way"
        # site keywords
        keywords = ["Matteo Totaro","Matteo","Totaro","Matteo Totaro website","mtotaro","Engineering","Electronic Engineer","Reggio Emilia","Università degli studi di Modena e Reggio Emilia","Hugo","Photography","Reflex","Photo","Engineering","Blog","Personal Blog","Photo Gallery","Museum","Travels","Barcelona","Venice","Italy","Amsterdam"]        
      # App icon config
      [languages.en.params.app]
        # optional site title override for the app when added to an iOS home screen or Android launcher
        title = "Matteo Totaro"
        # whether to omit favicon resource links
        noFavicon = false
        # modern SVG favicon to use in place of older style .png and .ico files
        svgFavicon = ""
        # Android browser theme color
        themeColor = "#ffffff"
        # Safari mask icon color
        iconColor = "#5bbad5"
        # Windows v8-10 tile color
        tileColor = "#da532c"
      # Search config
      [languages.en.params.search]
        enable = false
        # type of search engine ("lunr", "algolia")
        type = "algolia"
        # max index length of the chunked content
        contentLength = 4000
        # placeholder of the search bar
        placeholder = ""
        # max number of results length
        maxResultLength = 10
        # snippet length of the result
        snippetLength = 30
        # HTML tag name of the highlight part in results
        highlightTag = "em"
        # whether to use the absolute URL based on the baseURL in search index
        absoluteURL = false
        [languages.en.params.search.algolia]
          index = "index.en"
          appID = "PASDMWALPK"
          searchKey = "b42948e51daaa93df92381c8e2ac0f93"

        # Home Page Info
        [languages.en.params.home]
        # amount of RSS pages
        rss = 10
        # Home page profile
          [languages.en.params.home.profile]
            enable = true
            # Gravatar Email for preferred avatar in home page
            gravatarEmail = ""  #putting the email kills the imageavatar
            # URL of avatar shown in home page
            avatarURL = "/images/avatar.jpg"
            # title shown in home page (HTML format is supported)
            title = "Matteo Totaro"
            # subtitle shown in home page
            subtitle = "Welcome to my space on the web"
            # whether to use typeit animation for subtitle
            typeit = true
            # whether to show social links
            social = true
            # disclaimer (HTML format is supported)
            disclaimer = ""
          # Home Page Posts
          [languages.en.params.home.posts] #if enabled it shows list of posts on the home page
                                            #controllare se possibile far vedere solo le immagini
            enable = true
            # special amount of posts in each home posts page
            paginate = 6
        # Social config in home page
        [languages.en.params.social]
          #GitHub = "xxxx"
          Twitter = "matteojtotaro"
          Instagram = "matt.totaro"
          Email = "tmatteos@gmail.com"
          Linkedin = "m-totaro"
          # Spotify = "xxxx"

  [languages.it]  ##to add another language in the main menù i need to repeat this language.it menu.starter
    weight = 2
    # language code
    languageCode = "it"
    # website title
    title = "Matteo Totaro"
    # language name
    languageName = "Italiano"
    # whether to include Chinese/Japanese/Korean
    hasCJKLanguage = false
    # default amount of posts in each pages
    paginate = 12
    # [UA-XXXXXXXX-X] google analytics code
    googleAnalytics = "UA-164939128-1"
    # copyright description used only for seo schema
    copyright = "This work is licensed under a Creative Commons Attribution-NonCommercial 4.0 International License."
    
    # Menu Info
    [languages.it.menu]
      [[languages.it.menu.main]]
        identifier = "posts"
        # you can add extra information before the name (HTML format is allowed), such as icons
        pre = ""
        # you can add extra information after the name (HTML format is supported), such as icons
        post = ""
        name = "Galleria"
        url = "/posts/"
        # title will be shown when you hover on this menu link.
        title = ""
        weight = 1
           
      [[languages.it.menu.main]]
        identifier = "about"
        pre = ""
        post = ""
        name = "About"
        url = "/about/"
        title = ""
        weight = 2
      
      [languages.it.params]
        # site description
        description = "Angolo personale nel vasto web con una raccolta di scatti in un format molto minimal"
        # site keywords
        keywords = ["Matteo Totaro","Matteo","Totaro","Matteo Totaro sito web","mtotaro","Ingegneria","Ingegnere Elettronico","Reggio Emilia","Università degli studi di Modena e Reggio Emilia","Hugo","Fotografia","Reflex","Foto","Ingegneria","Blog","Blog personale","Galleria Fotografica","Museo","Viaggi","Barcellona","Venezia","Italia","Amsterdam"]
        # App icon config
        [languages.it.params.app]
        # optional site title override for the app when added to an iOS home screen or Android launcher
        title = "Matteo Totaro"
        # whether to omit favicon resource links
        noFavicon = false
        # modern SVG favicon to use in place of older style .png and .ico files
        svgFavicon = ""
        # Android browser theme color
        themeColor = "#ffffff"
        # Safari mask icon color
        iconColor = "#5bbad5"
        # Windows v8-10 tile color
        tileColor = "#da532c"
        
        # Search config
        [languages.it.params.search]
        enable = false
        # type of search engine ("lunr", "algolia")
        type = "algolia"
        # max index length of the chunked content
        contentLength = 4000
        # placeholder of the search bar
        placeholder = ""
        # max number of results length
        maxResultLength = 10
        # snippet length of the result
        snippetLength = 30
        # HTML tag name of the highlight part in results
        highlightTag = "em"
        # whether to use the absolute URL based on the baseURL in search index
        absoluteURL = false
          [languages.it.params.search.algolia]
            index = "index.en"
            appID = "PASDMWALPK"
            searchKey = "b42948e51daaa93df92381c8e2ac0f93"

        # Home Page Info
        [languages.it.params.home]
        # amount of RSS pages
        rss = 10
        # Home page profile
          [languages.it.params.home.profile]
            enable = true
            # Gravatar Email for preferred avatar in home page
            gravatarEmail = ""  #putting the email kills the imageavatar
            # URL of avatar shown in home page
            avatarURL = "/images/avatar.jpg"
            # title shown in home page (HTML format is supported)
            title = "Matteo Totaro"
            # subtitle shown in home page
            subtitle = "Benvenuti nel mio angolo sul web"
            # whether to use typeit animation for subtitle
            typeit = true
            # whether to show social links
            social = true
            # disclaimer (HTML format is supported)
            disclaimer = ""
          # Home Page Posts
          [languages.it.params.home.posts] #if enabled it shows list of posts on the home page
                                            #controllare se possibile far vedere solo le immagini
            enable = true
            # special amount of posts in each home posts page
            paginate = 6

        # Social config in home page
        [languages.it.params.social]
          #GitHub = "xxxx"
          Linkedin = "m-totaro"
          Twitter = "matteojtotaro"
          Instagram = "matt.totaro"
          #Spotify = ""
          Email = "tmatteos@gmail.com"



[params]
  # LoveIt theme version
    version = "0.2.X"
    # site default theme ("light", "dark", "auto")
    defaultTheme = "light"
  # public git repo url only then enableGitInfo is true
    gitRepo = "https://github.com/dillonzq/LoveIt"
  # which hash function used for SRI, when empty, no SRI is used ("sha256", "sha384", "sha512", "md5")
    fingerprint = ""
  # date format
    dateFormat = "02/01/2006"
  # website images for Open Graph and Twitter Cards
    images = ["/cover.jpg"]

  # Header info
  [params.header]
    # desktop header mode ("fixed", "normal", "auto")
    desktopMode = "fixed"
    # mobile header mode ("fixed", "normal", "auto")
    mobileMode = "auto"
    [params.header.title]
      # URL of the LOGO
      logo = "/static/LogoMT_127_40.png"
      # title name
      name = "Matteo Totaro"
      # you can add extra information before the name (HTML format is supported), such as icons
      pre = ""
      # you can add extra information after the name (HTML format is supported), such as icons
      post = ""
      # whether to use typeit animation for title name
      typeit = false

  # Footer Copyright Info
  [params.footer]
    enable = true
    # Custom content (HTML format is supported)
    custom = ''
    # whether to show Hugo and theme info
    hugo = false
    # whether to show copyright info
    copyright = true
    # whether to show the author
    author = true
    # site creation time
    since = 2020
    # ICP info only in China (HTML format is allowed)
    icp = ""
    # license info (HTML format is allowed)
    license= ''

  
  [params.section]
    # special amount of posts in each section page
    paginate = 20
    # date format (month and day)
    dateFormat = "02/01"
    # amount of RSS pages
    rss = 10

  # List (category or tag) page config
  [params.list]
    # special amount of posts in each list page
    paginate = 20
    # date format (month and day)
    dateFormat = "02/01/2006"
    # amount of RSS pages
    rss = 10


# Page config
  [params.page]
    # whether to hide a page from home page
    hiddenFromHomePage = false
    # whether to hide a page from search results
    hiddenFromSearch = false
    # whether to enable twemoji
    twemoji = false
    # whether to enable lightgallery
    lightgallery = true
    # whether to enable the ruby extended syntax
    ruby = false
    # whether to enable the fraction extended syntax
    fraction = false
    # whether to enable the fontawesome extended syntax
    fontawesome = false
    # whether to show link to Raw Markdown content of the content
    linkToMarkdown = false
    # whether to show the full text content in RSS
    rssFullText = true
    # Table of the contents config

    [params.page.toc]
      # whether to enable the table of the contents
      enable = false
      # whether to make the table of the contents automatically collapsed
      auto = true

    # Code config
    [params.page.code]
      # whether to show the copy button of the code block
      copy = true
      # the maximum number of lines of displayed code by default
      maxShownLines = 10
   
    # KaTeX mathematical formulas config (KaTeX https://katex.org/)
    [params.page.math]
      enable = false
      # default block delimiter is $$ ... $$ and \\[ ... \\]
      blockLeftDelimiter = ""
      blockRightDelimiter = ""
      # default inline delimiter is $ ... $ and \\( ... \\)
      inlineLeftDelimiter = ""
      inlineRightDelimiter = ""
      # KaTeX extension copy_tex
      copyTex = true
      # KaTeX extension mhchem
      mhchem = true
    # Mapbox GL JS config (Mapbox GL JS https://docs.mapbox.com/mapbox-gl-js)
    [params.page.mapbox]
      # access token of Mapbox GL JS
      accessToken = "pk.eyJ1IjoiZGlsbG9uenEiLCJhIjoiY2s2czd2M2x3MDA0NjNmcGxmcjVrZmc2cyJ9.aSjv2BNuZUfARvxRYjSVZQ"
      # style for the light theme
      lightStyle = "mapbox://styles/mapbox/light-v10?optimize=true"
      # style for the dark theme
      darkStyle = "mapbox://styles/mapbox/dark-v10?optimize=true"
      # whether to add NavigationControl (https://docs.mapbox.com/mapbox-gl-js/api/#navigationcontrol)
      navigation = true
      # whether to add GeolocateControl (https://docs.mapbox.com/mapbox-gl-js/api/#geolocatecontrol)
      geolocate = true
      # whether to add ScaleControl (https://docs.mapbox.com/mapbox-gl-js/api/#scalecontrol)
      scale = true
      # whether to add FullscreenControl (https://docs.mapbox.com/mapbox-gl-js/api/#fullscreencontrol)
      fullscreen = true

    # Social share links in post page
    [params.page.share]
      enable = true
      Twitter = true
      Facebook = true
      Linkedin = true
      Whatsapp = true
      Pinterest = true
      Tumblr = true
      HackerNews = false
      Reddit = true
      VK = false
      Buffer = false
      Xing = false
      Line = false
      Instapaper = false
      Pocket = false
      Digg = false
      Stumbleupon = false
      Flipboard = false
      Weibo = false
      Renren = false
      Myspace = false
      Blogger = false
      Baidu = false
      Odnoklassniki = false
      Evernote = false
      Skype = false
      Trello = false
      Mix = false
    
    # Comment config
    [params.page.comment]
      enable = false
      # Disqus comment config (https://disqus.com/)
      [params.page.comment.disqus]
        enable = false
        # Disqus shortname to use Disqus in posts
        shortname = ""
      # Gitalk comment config (https://github.com/gitalk/gitalk)
      [params.page.comment.gitalk]
        enable = false
        owner = ""
        repo = ""
        clientId = ""
        clientSecret = ""
      # Valine comment config (https://github.com/xCss/Valine)
      [params.page.comment.valine]
        enable = false
        appId = "QGzwQXOqs5JOhN4RGPOkR2mR-MdYXbMMI"
        appKey = "WBmoGyJtbqUswvfLh6L8iEBr"
        placeholder = ""
        avatar = "mp"
        meta= ""
        pageSize = 10
        lang = ""
        visitor = true
        recordIP = true
        highlight = true
        enableQQ = false
        serverURLs = ""
      # Facebook comment config (https://developers.facebook.com/docs/plugins/comments)
      [params.page.comment.facebook]
        enable = false
        width = "100%"
        numPosts = 10
        appId = ""
        languageCode = ""
      # Telegram comments config (https://comments.app/)
      [params.page.comment.telegram]
        enable = false
        siteID = ""
        limit = 5
        height = ""
        color = ""
        colorful = true
        dislikes = false
        outlined = false
      # Commento comments config (https://commento.io/)
      [params.page.comment.commento]
        enable = false

    # Third-party library config
    [params.page.library]
      [params.page.library.css]
        # someCSS = "some.css"
        # located in "assets/" 位于 "assets/"
        # Or cdn
        # BootstrapCss = "bootstrap.min.css"
        # BootstrapGridCss = "bootstrap-grid.min.css"
      [params.page.library.js]
        # someJavascript = "some.js"
        # located in "assets/" 位于 "assets/"
        # Or 或者
        # jQuery = "jquery-3.5.1.min.js"
        # PopperJs = "popper.min.js"
        # BootstrapJs = "bootstrap.min.js"
        # BootstrapBundleJs = "bootstrap.bundle.min.js"
    # SEO config
    [params.page.seo]
      # image URL
      images = []
      # Publisher info
      [params.page.seo.publisher]
        name = "Matteo Totaro"
        logoUrl = "avatar.jpg"

  # TypeIt config
  [params.typeit]
    # typing speed between each step (measured in milliseconds)
    speed = 100
    # blinking speed of the cursor (measured in milliseconds)
    cursorSpeed = 1000
    # character used for the cursor (HTML format is supported)
    cursorChar = "|"
    # cursor duration after typing finishing (measured in milliseconds, "-1" means unlimited)
    duration = -1

  # site verification code for Google/Bing/Yandex/Pinterest/Baidu
  [params.verification]
    google = "FJlJwxMy2rXULl5c91sw_X3US7EI2RZSuxVGwmLx-N4"
    bing = ""
    yandex = ""
    pinterest = ""
    baidu = ""

  # Site SEO config
  [params.seo]
    # image URL
    image = "/images/cover.jpg"
    # thumbnail URL
    thumbnailUrl = "logoMT.png"

 # Analytics config
  [params.analytics]
    enable = true
    # Google Analytics id privacy
    [params.analytics.google]
      id = ""
      # whether to anonymize IP
      anonymizeIP = true
      # whether to use cookie
      cookie = true
 
  # Cookie consent config
  [params.cookieconsent]
    enable = false
    # text strings used for Cookie consent banner
    [params.cookieconsent.content]
      message = "We use first and third party cookies for several reasons. Some cookies are required for technical reasons in order for our Websites to operate, and we refer to these as <i>essential</i> or <i>strictly necessary</i> cookies. Other cookies also enable us to track and target the interests of our users to enhance the experience on our Online Properties. <a href=https://mtotaro.com/privacypolicy/>Click to learn more about it</a>"
      dismiss = "Dismiss"
      link = "What are cookies?"
   
   [services]
   #If simple mode is enabled, a static and no-JS version of the Instagram image card will be built. 
   #Note that this only supports image cards and the image itself will be fetched from Instagram’s servers.
      [services.instagram]
          disableInlineCSS = false

  # CDN config for third-party library files

  [params.cdn]
    # CDN data file name, disabled by default
    # ("jsdelivr.yml")
    # located in "themes/LoveIt/assets/data/cdn/" directory
    # you can store your own data files in the same path under your project:
    # "assets/data/cdn/"
    data = "jsdelivr.yml"
    mydata = "mycdns.yml"

  # Compatibility config
  [params.compatibility]
    # whether to use Polyfill.io to be compatible with older browsers
    polyfill = false
    # whether to use object-fit-images to be compatible with older browsers
    objectFit = true

# Markup related configuration in Hugo
[markup]
  # Syntax Highlighting (https://gohugo.io/content-management/syntax-highlighting)
  [markup.highlight]
    codeFences = true
    guessSyntax = true
    lineNos = true
    lineNumbersInTable = true
    # false is a necessary configuration (https://github.com/dillonzq/LoveIt/issues/158)
    noClasses = false
  # Goldmark is from Hugo 0.60 the default library used for Markdown
  [markup.goldmark]
    [markup.goldmark.extensions]
      definitionList = true
      footnote = true
      linkify = true
      strikethrough = true
      table = true
      taskList = true
      typographer = true
    [markup.goldmark.renderer]
      # whether to use HTML tags directly in the document
      unsafe = true
  # Table Of Contents settings
  [markup.tableOfContents]
    startLevel = 2
    endLevel = 6

[author]
  name = "Matteo Totaro"
  email = "tmatteos@gmail.com"
  link = "/about/"
  
# Sitemap Info
[sitemap]
  changefreq = "monthly"
  filename = "sitemap.xml"
  priority = 0.5

# Permalinks Info (https://gohugo.io/content-management/urls/#permalinks)
[Permalinks]
  # posts = ":year/:month/:filename"
  posts = ":filename"

# Privacy Info (https://gohugo.io/about/hugo-and-gdpr/)
[privacy]
  # privacy of the Google Analytics (replaced by params.analytics.google)
  [privacy.googleAnalytics]
    # ...
  [privacy.twitter]
    enableDNT = true
  [privacy.youtube]
    privacyEnhanced = true

# Options to make output .md files
[mediaTypes]
  [mediaTypes."text/plain"]
    suffixes = ["md"]

# Options to make output .md files
[outputFormats.MarkDown]
  mediaType = "text/plain"
  isPlainText = true
  isHTML = false

# Options to make hugo output files???
[outputs]
  home = ["HTML", "RSS"]
  page = ["HTML", "MarkDown"]
  section = ["HTML", "RSS"]
  taxonomy = ["HTML", "RSS"]
  taxonomyTerm = ["HTML"]